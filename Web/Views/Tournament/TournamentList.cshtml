@model IEnumerable<Entities.Tournament>

Current Tournaments:
<div class="tournaments">
    @foreach (var tournament in Model)
    {
        <ul class="tournament">
            <li>
                @Html.ActionLink(tournament.Name, "Detail", new { controller = "Tournament", id = tournament.Id }) - @tournament.Status.ToString()
            </li>
        </ul>
    }
</div>